# TypeScript 📘

**Purpose:** A collection of TypeScript learning materials, exercises, and example projects to help you learn and practice modern TypeScript in real-world scenarios.

---

## 📁 Structure

- `Learn_Typescript/` — Lesson folders and example code (add new lessons here).
- `More will be added in the future.`
---

## 🚀 Getting started

Prerequisites:

- Node.js (LTS recommended)
- npm or yarn

Quick setup (local project install):

```bash
# install TypeScript and ts-node for running examples
npm install --save-dev typescript ts-node

# initialize a tsconfig.json if needed
npx tsc --init
```

Run a TypeScript file directly:

```bash
npx ts-node src/example.ts
```

Compile to JavaScript:

```bash
npx tsc
node build/index.js  # or the output path from your tsconfig
```

---

## 🔧 Recommended tools

- **VS Code** with the built-in TypeScript support
- **ESLint** for linting (use `@typescript-eslint` plugin)
- **Prettier** for consistent formatting

> Tip: Enable `editor.formatOnSave` and configure ESLint to run on save for fast feedback.

---

## 📚 Resources & learning

- Official TypeScript Handbook — a great reference for language features.
- TypeScript Deep Dive — practical guide and patterns.
- Playgrounds (online) — quick way to experiment with TS snippets.
- Courses.

---

## 🤝 Contributing

Contributions welcome — open an issue or submit a pull request with:

- A clear description of the exercise or fix
- Test cases or example inputs where relevant

---
